# PLAN.md: Создание micro-MVP «Кто выгуливает Бориса»

## 1. Текущий статус

- Приоритет: собрать минимально работающий главный сценарий с постоянным состоянием в SQLite.
- Текущее состояние: micro-MVP реализован; главный сценарий прогулки, атомарный отказ при конфликте, постоянство SQLite, последнее кормление, адаптивность и итоговый smoke-check проверены и приняты студентом.
- Основная цель цикла: реализовать и фактически проверить продукт от локального запуска до итогового smoke-check.
- Обязательные ограничения:
  - использовать только Node.js, npm и SQLite;
  - хранить постоянное состояние только в SQLite;
  - защищать повторную запись в слот атомарно на сервере;
  - использовать только синтетические ФИО и данные;
  - не добавлять функции вне `spec.md`;
  - не выполнять commit, push, публикацию, удаление данных или деплой без отдельного явного запроса;
  - выполнять за одну итерацию только один явно выбранный шаг;
  - не считать шаг завершённым без фактической проверки.
- Основной контекст проекта: `spec.md`, `AGENTS.md`, `README.md` и этот `PLAN.md`.
- Текущий активный шаг: отсутствует; все пункты 1–14 выполнены и приняты студентом.

## 1.1. Главный сценарий прогулки

1. Пользователь запускает приложение локально предусмотренной npm-командой.
2. Открывает общую страницу «Кто выгуливает Бориса».
3. Видит текущую дату.
4. Видит ровно пять слотов прогулки: `09:00`, `11:00`, `13:00`, `15:00` и `17:00`.
5. Отличает свободные слоты от занятых.
6. Выбирает свободный слот.
7. Вводит синтетическое ФИО и подтверждает запись.
8. Видит, что слот стал занятым и показывает введённое ФИО.
9. Из ранее открытой страницы во втором браузере повторно пытается занять тот же слот другим ФИО.
10. Получает понятный серверный отказ, а первоначальная запись не меняется.
11. Обновляет страницу или открывает приложение во втором браузере.
12. Видит сохранённое состояние слотов.
13. Проходит главный сценарий на широком и узком экране без помех.

Дополнительная проверка кормления вне главного сценария:

1. Пользователь видит понятное пустое состояние карточки до первой отметки.
2. Отмечает кормление синтетическим ФИО.
3. Видит в карточке последнего кормления новое ФИО и время.
4. Обновляет страницу и видит сохранённое состояние карточки.

## 2. Требования к результату

### Требования к `spec.md`

- Считать `spec.md` единственным источником продуктовых требований.
- Не изменять требования и не расширять скоуп.
- Реализовать SQLite-схему `walk_slots` и `feedings` без смысловых отклонений.
- Создавать для текущей даты ровно пять фиксированных слотов.
- Проверять непустое ФИО после удаления лишних пробелов.
- Реализовать атомарную серверную защиту занятого слота.
- Подтвердить все семь DoD фактическими проверками.

### Требования к `README.md`

- До первой успешной проверки не указывать предполагаемую команду запуска как рабочую.
- После проверки основы приложения записать точную команду локального запуска.
- После последней принятой правки обновить:
  - фактический способ запуска;
  - главный сценарий;
  - структуру проекта;
  - технические и продуктовые границы.
- Не заявлять о production-ready состоянии.

### Требования к `AGENTS.md`

- Соблюдать постоянные правила работы агента.
- Перед каждым шагом читать `spec.md`, `README.md`, `PLAN.md` и только необходимые файлы проекта.
- До изменений называть затрагиваемые файлы, ожидаемый эффект и способ проверки.
- После изменений показывать результат и выполнять доступную проверку.
- Не переходить к следующему шагу автоматически.
- Не выполнять commit, push, публикацию, удаление данных или деплой без явного запроса.

### Требования к `PLAN.md`

- Хранить номер текущего активного шага.
- Одновременно считать активным только один шаг.
- После каждой итерации обновлять статус соответствующего шага.
- Вместо ожидаемого результата записывать фактически выполненную проверку.
- Не отмечать шаг выполненным, если проверка не запускалась или завершилась ошибкой.
- После завершения шага указывать:
  - какие файлы изменены;
  - какая проверка выполнена;
  - фактический результат;
  - известные ограничения или ошибки.

### Границы текущего этапа

Текущая итерация завершилась принятием шага 14: просмотрены код, зависимости, документация, отслеживаемый состав проекта и итоговый diff; выполнен финальный локальный запуск, результат принят студентом.

В рамках этой итерации не изменялись код и поведение приложения, не добавлялись зависимости, авторизация, роли, отмена записей, уведомления, календарь или другие соседние функции; публикация и commit не выполнялись.

## 3. Что делаем по шагам

- [x] 1. Создать минимальную основу Node.js-приложения

  - Статус: `Выполнено и принято студентом`.
  - Действие: создать `package.json`, минимальный сервер, npm-команду запуска и только необходимые файлы проекта.
  - Создано: `package.json` с командой `npm start`; `package-lock.json`; `server.js` с минимальным HTTP-сервером и стартовой HTML-страницей; `Containerfile` для штатной публикации Vibe.
  - Автоматическая проверка: Node.js `v24.18.0`, npm `11.16.0`; `npm install` выполнен успешно, создан lock-файл, аудит сообщил `0 vulnerabilities`; обычный `npm start` запустил сервер на выданном окружением порту `8373`; запрос `GET /` вернул `HTTP 200` и HTML-страницу с заголовком «Кто выгуливает Бориса». Контейнерный образ собран и запущен непривилегированным пользователем в режиме `read-only`; внутренний health-check `GET /` на порту `8080` вернул `HTTP 200`.
  - Публикация: выполнена штатным publisher на `https://vibe-apps.aikibox.ru/sprint-1/`; внутренний health-check и статус deployment успешны.
  - Ручная проверка: опубликованная страница `https://vibe-apps.aikibox.ru/sprint-1/` проверена вручную студентом; результат принят.
  - Известные ограничения: первая попытка запуска с привязкой к `0.0.0.0` получила `EADDRINUSE`, потому что шлюз среды уже занимает внешний интерфейс на порту `8373`; после минимальной корректировки привязки к `127.0.0.1` обычный запуск успешен. SQLite, слоты прогулок и карточка кормления относятся к следующим шагам и пока отсутствуют.
  - Проверка:
    - установить зависимости;
    - выполнить npm-команду запуска;
    - открыть локальный адрес;
    - проверить ответ сервера;
    - просмотреть diff на отсутствие лишней инфраструктуры.

- [x] 2. Подключить SQLite и реализовать схему

  - Статус: `Выполнено и принято студентом`.
  - Действие: установить совместимый SQLite-драйвер, создавать SQLite-файл и реализовать таблицы `walk_slots` и `feedings` по `spec.md`.
  - Создано: модуль `database.js` на встроенном в Node.js 24 драйвере `node:sqlite`, создающий локальный файл `boris.sqlite` и таблицы `walk_slots` и `feedings`; инициализация базы при старте сервера; правила игнорирования файла базы и его служебных файлов; включение `database.js` в контейнерный образ; постоянный записываемый каталог `/data` для опубликованного контейнера с путём базы `/data/boris.sqlite`.
  - Автоматическая проверка: чистый `npm ci` выполнен успешно, аудит сообщил `0 vulnerabilities`; приложение запущено с `HOST=127.0.0.1 PORT=38427`, запрос `GET /` вернул `HTTP 200` и исходную стартовую страницу; создан файл `boris.sqlite` размером 20480 байт; через метаданные SQLite подтверждены обе таблицы и все поля из `spec.md`; пробные нарушения `UNIQUE (walk_date, slot_time)` и согласованности `booked_by`/`booked_at` отклонены SQLite; проверочные изменения откатились, обе таблицы остались пустыми; в коде приложения не найдено `localStorage` или хранилище записей в массиве, `Map` либо `Set`.
  - Ручная проверка: студент проверил рабочий `boris.sqlite`; подтверждены правильные поля таблиц `walk_slots` и `feedings`, запрет дубликата сочетания даты и времени, запрет ФИО без времени бронирования и обязательность полей кормления. Студент принял шаг.
  - Известные ограничения: пять слотов текущей даты и пользовательские действия ещё не реализованы, поскольку относятся к следующим шагам. Первая попытка использовать внешний пакет `better-sqlite3` не прошла повторяемый чистый `npm ci` из-за отсутствия инструмента нативной сборки; пакет удалён, вместо него используется встроенный в Node.js 24 драйвер `node:sqlite`. Локальная проверка контейнерной сборки остановилась из-за ограничения Podman/cgroup на VPS, поэтому контейнерная сборка и health-check должны быть подтверждены штатным publisher.
  - Проверка:
    - запустить приложение;
    - убедиться, что SQLite-файл создан;
    - проверить наличие обеих таблиц;
    - проверить поля, `UNIQUE` и `CHECK`;
    - убедиться, что состояние не хранится в памяти или `localStorage`.

- [x] 3. Создавать пять слотов текущей даты

  - Статус: `Выполнено и принято студентом`.
  - Действие: при запуске добавлять отсутствующие слоты `09:00`, `11:00`, `13:00`, `15:00` и `17:00` для текущей даты.
  - Создано: в `database.js` добавлены вычисление текущей локальной даты и транзакционная идемпотентная вставка пяти фиксированных свободных слотов через `INSERT OR IGNORE`.
  - Автоматическая проверка: приложение дважды запущено с отдельным временным SQLite-файлом; после первого и второго запуска в `walk_slots` находились ровно пять строк за `2026-09-16` со временем `09:00`, `11:00`, `13:00`, `15:00` и `17:00`; во всех строках `booked_by` и `booked_at` равны `NULL`; повторный запуск не создал дубликаты.
  - Ручная проверка: студент запустил приложение командой `HOST=127.0.0.1 PORT=3000 npm start` и получил сообщение `Приложение запущено: http://localhost:3000`; после запуска в рабочем `boris.sqlite` подтверждены ровно пять свободных слотов за `2026-09-16` с требуемыми временами и отсутствие дубликатов. Студент принял шаг.
  - Известные ограничения: получение и отображение слотов на странице ещё не реализованы, поскольку относятся к следующему продуктовому шагу.
  - Проверка:
    - проверить записи в SQLite;
    - повторно запустить приложение;
    - убедиться, что дубликаты не появились;
    - подтвердить точное количество и времена слотов.

- [x] 4. Завершить основу постоянного состояния

  - Статус: `Выполнено и принято студентом`.
  - Действие: проверить фазу основы целиком и обновить документацию фактическими результатами.
  - Создано и изменено: в `README.md` добавлены проверенные команды установки и локального запуска, описание создаваемого SQLite-файла, граница текущей реализации и актуальная структура проекта; в `PLAN.md` записаны фактические результаты проверки пункта 4.
  - Автоматическая проверка: `npm ci` выполнен успешно, аудит сообщил `0 vulnerabilities`; приложение дважды запущено с `HOST=127.0.0.1 PORT=39241` и отдельным временным SQLite-файлом, оба запроса `GET /` вернули `HTTP 200`; подтверждены таблицы `walk_slots` и `feedings` со всеми полями из `spec.md`; нарушения `UNIQUE (walk_date, slot_time)`, согласованности `booked_by`/`booked_at` и `NOT NULL` для кормления отклонены SQLite, а проверочные изменения откатились; после первого и повторного запуска в базе остались ровно пять свободных слотов за `2026-09-16` со временем `09:00`, `11:00`, `13:00`, `15:00` и `17:00`, дубликатов нет; общий Git diff просмотрен, изменения за пределами принятых шагов 2–3 и документации шага 4 не вносились.
  - Ручная проверка: студент ранее запустил приложение командой `HOST=127.0.0.1 PORT=3000 npm start`, подтвердил запуск и проверил рабочий `boris.sqlite`, схему, пять свободных слотов текущей даты и отсутствие дубликатов; студент подтвердил, что эта проверка покрывает пункт 4, и принял результат.
  - Известные ограничения: страница пока показывает только сообщение о запуске основы; получение и отображение слотов относятся к шагу 5. В этой итерации контейнерная сборка и опубликованная версия не проверялись.
  - Проверка:
    - повторить запуск;
    - проверить SQLite-файл, схему и пять слотов;
    - просмотреть общий diff;
    - внести проверенную команду запуска в README;
    - записать фактические результаты в PLAN.

- [x] 5. Реализовать получение и отображение слотов

  - Статус: `Выполнено и принято студентом`.
  - Действие: добавить серверное получение слотов из SQLite и общую страницу с текущей датой и пятью слотами.
  - Создано и изменено: в `database.js` добавлена упорядоченная выборка слотов по дате; в `server.js` добавлены получение слотов из SQLite при запросе общей страницы и серверная HTML-разметка с текущей датой и пятью временами; визуальные состояния, ФИО и действия записи не добавлялись.
  - Автоматическая проверка: `node --check database.js` и `node --check server.js` завершились успешно; приложение запущено с `HOST=127.0.0.1 PORT=39317` и отдельным временным SQLite-файлом; `GET /` вернул `HTTP 200`; в HTML найдена дата `2026-09-16` и ровно пять слотов `09:00`, `11:00`, `13:00`, `15:00`, `17:00`; дата и времена полностью совпали со строками временной SQLite-базы.
  - Ручная проверка: текущая версия опубликована на `https://vibe-apps.aikibox.ru/sprint-1/`; студент подтвердил проверку опубликованной страницы и принял шаг.
  - Известные ограничения: свободные и занятые слоты пока не различаются визуально, ФИО не показываются и записаться нельзя — это относится к следующим пунктам PLAN.
  - Проверка:
    - открыть страницу;
    - проверить текущую дату;
    - пересчитать слоты;
    - сравнить состояние страницы с данными SQLite.

- [x] 6. Различить свободные и занятые слоты

  - Статус: `Выполнено и принято студентом`.
  - Действие: добавить понятные визуальные состояния и показывать ФИО в занятом слоте.
  - Создано и изменено: в `server.js` добавлены отдельные карточки свободного и занятого слота с явными подписями «Свободен» и «Занят», различающимися цветами и рамками; занятый слот показывает экранированное ФИО из SQLite; формы, кнопки и серверная запись не добавлялись.
  - Автоматическая проверка: `node --check server.js` и `node --check database.js` завершились успешно; приложение запущено с отдельным временным SQLite-файлом, где слот `11:00` занят синтетическим сотрудником «Анна Тестова», а остальные четыре слота свободны; `GET /` вернул `HTTP 200`; в HTML найдены ровно пять карточек, четыре состояния `Свободен`, одно состояние `Занят`, ФИО «Анна Тестова» и отдельные стили обоих состояний; состояние совпало с SQLite; формы и кнопки записи отсутствуют.
  - Ручная проверка: студент проверил опубликованную страницу с занятым слотом `11:00` на имя «Анна Тестова» и четырьмя свободными слотами; подтверждены визуально различимые состояния, текстовые подписи «Занят» и «Свободен» и правильное ФИО; студент принял шаг.
  - Известные ограничения: занять слот через страницу пока нельзя, а конфликт повторной записи не обрабатывается — это относится к пунктам 7 и 8 PLAN.
  - Проверка:
    - открыть страницу с одним занятым и одним свободным слотом;
    - убедиться, что состояния визуально различаются;
    - проверить отображение ФИО занятого слота.

- [x] 7. Реализовать запись в свободный слот

  - Статус: `Выполнено и принято студентом`.
  - Действие: добавить ввод ФИО, серверный запрос записи и обновление интерфейса после успеха.
  - Создано и изменено: в `server.js` для каждого свободного слота добавлена форма ФИО, серверная обработка `POST /walks`, проверка непустого после удаления крайних пробелов значения, сообщение об ошибке рядом с нужным полем на основной странице и перенаправление на обновлённую страницу после успеха; в `database.js` добавлено сохранение ФИО и времени бронирования одной условной операцией только для свободного слота. Отдельный пользовательский сценарий конфликта не добавлялся.
  - Автоматическая проверка: `node --check database.js` и `node --check server.js` завершились успешно; приложение запущено с отдельным чистым временным SQLite-файлом; исходная страница вернула `HTTP 200` и пять форм; отправка строки из пробелов вернула основную HTML-страницу с `HTTP 400`, пятью слотами, пятью формами, сообщением `Введите непустое ФИО` рядом с нужным полем и признаком `aria-invalid`, не изменив SQLite; следующая отправка `  Анна Тестова  ` в слот `09:00` вернула `HTTP 303`, после нового `GET /` страница показала занятый слот с ФИО «Анна Тестова» и четыре оставшиеся формы, а SQLite сохранил обрезанное ФИО и непустое время бронирования. Первый прогон обнаружил ошибочное сравнение числового `changes` с `BigInt`, из-за чего успешная запись получала ответ `409`; сравнение исправлено, вся проверка повторена на новой чистой базе и прошла. При первой ручной проверке опубликованной версии обнаружено, что серверный отказ отображался отдельной белой страницей; ошибка исправлена, повторная изолированная проверка основной страницы и успешной записи прошла.
  - Ручная проверка: студент проверил опубликованную страницу, записал «Анну Тестову» в слот `09:00`, подтвердил обновлённое занятое состояние и встроенное сообщение `Введите непустое ФИО` при отправке строки из пробелов; удалённая SQLite-база дополнительно проверена в режиме чтения и содержит за `2026-09-17` слот `09:00`, ФИО `Анна Тестова` и непустое время бронирования; студент принял шаг.
  - Известные ограничения: повторная и конкурентная запись, понятное сообщение о конфликте и сохранность первоначального ФИО при конфликте ещё не проверены — это пункт 8 PLAN. Условное обновление уже не позволяет серверу перезаписать занятый слот, чтобы промежуточная версия не нарушала обязательное правило `AGENTS.md`.
  - Проверка:
    - записать «Анна Тестова» в свободный слот;
    - проверить новое состояние интерфейса;
    - проверить запись в SQLite;
    - отдельно проверить отказ для пустого ФИО и строки из пробелов.

- [x] 8. Защитить занятый слот от повторной записи

  - Статус: `Выполнено и принято студентом`.
  - Действие: реализовать атомарное изменение только свободного слота и серверный ответ о конфликте.
  - Создано и изменено: в `server.js` сохранено атомарное условное обновление слота через `database.js`; при конфликте сервер повторно получает актуальные слоты из SQLite и возвращает основную HTML-страницу с HTTP `409`, занятым состоянием слота и сообщением `Этот слот уже занят. Первоначальная запись сохранена.`; в `PLAN.md` записан фактический результат итерации. Новые файлы не создавались.
  - Автоматическая проверка: `node --check database.js`, `node --check server.js` и `git diff --check` завершились успешно; приложение запущено с `HOST=127.0.0.1 PORT=39418` и отдельным временным SQLite-файлом; заранее открытая страница вернула `HTTP 200`; первая запись «Анна Тестова» в слот `09:00` вернула `HTTP 303`; повторная отправка с той же устаревшей страницы именем «Илья Примеров» вернула основную HTML-страницу с `HTTP 409`, сообщением о конфликте, занятым слотом и ФИО «Анна Тестова»; чтение SQLite после конфликта подтвердило, что в слоте осталось имя «Анна Тестова» и непустое время бронирования; новый `GET /` также показал «Анна Тестова» и не содержал «Илья Примеров».
  - Ручная проверка: студент открыл опубликованную версию `https://vibe-apps.aikibox.ru/sprint-1/`, выполнил предложенный сценарий повторной записи из ранее открытой страницы второго браузера, подтвердил корректное поведение и принял шаг.
  - Известные ограничения: кормление и последующие пункты PLAN не реализовывались.
  - Проверка:
    - занять слот именем «Анна Тестова»;
    - из ранее открытой страницы во втором браузере попытаться занять тот же слот именем «Илья Примеров»;
    - проверить серверный ответ о конфликте;
    - проверить понятное сообщение в интерфейсе;
    - убедиться, что в интерфейсе и SQLite осталось имя «Анна Тестова».

- [x] 9. Добавить последнее кормление

  - Статус: `Выполнено и принято студентом`.
  - Действие: реализовать сохранение кормления, получение последней записи и компактную карточку на общей странице.
  - Создано и изменено: в `database.js` добавлены вставка технической записи кормления с текущим временем и получение последней записи по времени и идентификатору; в `server.js` добавлены компактная карточка «Последнее кормление», понятное пустое состояние, показ ФИО и времени последней отметки, форма ФИО, обработка `POST /feedings`, серверная проверка непустого после удаления крайних пробелов значения и встроенное сообщение об ошибке; в `PLAN.md` записан фактический результат итерации. Новые файлы и зависимости не создавались.
  - Автоматическая проверка: `node --check database.js`, `node --check server.js` и `git diff --check` завершились успешно; приложение запущено с `HOST=127.0.0.1 PORT=39419` и отдельным чистым временным SQLite-файлом; исходный `GET /` вернул `HTTP 200`, карточку с текстом `Кормление пока не отмечали.`, одну форму кормления и прежние пять слотов прогулок; отправка строки из пробелов вернула основную HTML-страницу с `HTTP 400`, встроенным сообщением `Введите непустое ФИО` и `aria-invalid`, оставив таблицу `feedings` пустой; отправка `  Илья Примеров  ` вернула `HTTP 303`, сохранила обрезанное ФИО и непустое время в SQLite, а следующий `GET /` показал ФИО и отформатированное время без пустого состояния; следующая отметка «Анна Тестова» вернула `HTTP 303`, стала последней в карточке, а SQLite содержал обе технические записи; дополнительно проверено, что страница по-прежнему содержит ровно пять слотов, отказ для пустого ФИО прогулки возвращает встроенную ошибку, а корректная запись на прогулку возвращает `HTTP 303`.
  - Ручная проверка: студент открыл опубликованную версию `https://vibe-apps.aikibox.ru/sprint-1/`, создал отметки кормления и через консоль прочитал удалённый SQLite в режиме `readOnly`; подтверждены две сохранённые записи «Илья Примеров» и «Анна Тестова», а карточка показала последнюю запись «Анна Тестова» с тем же временем; студент принял шаг.
  - Известные ограничения: отдельная проверка общего состояния между перезапусками и браузерами относится к пункту 10 PLAN; адаптивность относится к пункту 11.
  - Проверка:
    - при отсутствии кормлений проверить понятное пустое состояние карточки;
    - отметить кормление именем «Илья Примеров»;
    - проверить обновление карточки;
    - проверить запись в SQLite;
    - проверить отказ для пустого ФИО.

- [x] 10. Проверить постоянство общего состояния

  - Статус: `Выполнено и принято студентом`.
  - Действие: проверить сохранение прогулки и кормления между загрузками страницы и браузерами.
  - Создано и изменено: код приложения не изменялся; в `PLAN.md` записаны фактические результаты проверки постоянства состояния.
  - Автоматическая проверка: приложение запущено с `HOST=127.0.0.1 PORT=39420` и отдельным чистым временным SQLite-файлом; запись «Анна Тестова» в слот `11:00` и кормление «Илья Примеров» вернули `HTTP 303`, отобразились на странице и были найдены в SQLite с непустыми временами; первый процесс остановлен, затем приложение запущено новым процессом на `PORT=39421` с тем же SQLite-файлом; `GET /` вернул `HTTP 200`, ровно пять слотов, занятую прогулку «Анна Тестова», последнее кормление «Илья Примеров» и исходное время кормления; чтение SQLite в режиме `readOnly` после перезапуска подтвердило по одной сохранённой записи прогулки и кормления.
  - Ручная проверка: студент подтвердил предложенную проверку опубликованной страницы после обновления и во втором браузере и принял шаг.
  - Известные ограничения: адаптивность относится к пункту 11 PLAN.
  - Проверка:
    - создать запись на прогулку;
    - отметить кормление;
    - обновить страницу;
    - проверить сохранение обоих состояний;
    - открыть приложение во втором браузере или другом контролируемом запуске;
    - проверить то же состояние;
    - сопоставить результат с содержимым SQLite.

- [x] 11. Проверить широкую и узкую ширину экрана

  - Статус: `Выполнено и принято студентом`.
  - Действие: пройти главный сценарий на широком и узком экране и исправить только обнаруженные помехи.
  - Создано и изменено: в `server.js` добавлен один мобильный breakpoint до `600px`: карточка слота переходит из трёх колонок в одну, поля и кнопки прогулки и кормления располагаются вертикально, кнопки занимают доступную ширину, статус выравнивается по началу, а внешние и внутренние отступы уменьшаются; широкая компоновка не изменялась. В `PLAN.md` записан фактический результат итерации. Новые файлы и зависимости не создавались.
  - Автоматическая проверка: `node --check database.js`, `node --check server.js` и `git diff --check` завершились успешно; приложение запущено с `HOST=127.0.0.1 PORT=39422` и отдельным временным SQLite-файлом; HTML содержит `viewport` meta, breakpoint `@media (max-width: 600px)`, одноколоночную сетку слота и вертикальную компоновку полей; страница сохранила ровно пять слотов; запись «Анна Тестова» в `09:00` вернула `HTTP 303`; повторная запись «Илья Примеров» вернула `HTTP 409`, понятное сообщение и первоначальное ФИО; кормление «Илья Примеров» вернуло `HTTP 303`; итоговая страница показала прогулку и кормление. Браузерный навык не смог подключить визуальную вкладку локального приложения, поэтому фактическая визуальная проверка ширин не объявляется пройденной.
  - Ручная проверка: студент проверил опубликованную версию на широкой и узкой ширине, подтвердил корректное отображение и принял шаг.
  - Известные ограничения: итоговый smoke-check относится к пункту 12 PLAN.
  - Проверка:
    - проверить страницу на широкой ширине;
    - проверить страницу на узкой ширине;
    - выполнить запись в слот;
    - проверить сообщение о конфликте;
    - отдельно проверить, что карточка кормления не мешает главному сценарию;
    - убедиться, что элементы не перекрываются и не обрезаются.

- [x] 12. Выполнить итоговый smoke-check

  - Статус: `Выполнено и принято студентом`.
  - Действие: последовательно выполнить контролируемый smoke-check из `spec.md`.
  - Создано и изменено: код приложения не изменялся; в `PLAN.md` записаны фактические результаты итоговой проверки. Новые файлы и зависимости не создавались.
  - Автоматическая проверка: приложение запущено с `HOST=127.0.0.1 PORT=39423` и отдельным чистым временным SQLite-файлом; исходный `GET /` вернул `HTTP 200`, текущую дату `2026-09-17`, ровно пять слотов `09:00`, `11:00`, `13:00`, `15:00`, `17:00`, пустое состояние кормления и адаптивные правила; запись «Анна Тестова» в слот `13:00` вернула `HTTP 303`; повторная запись «Илья Примеров» вернула `HTTP 409`, понятное сообщение и исходное ФИО без конфликтного имени; следующий `GET /` сохранил прогулку; кормление «Илья Примеров» вернуло `HTTP 303`, а следующий `GET /` показал ФИО и время; SQLite в режиме `readOnly` содержал ровно пять слотов текущей даты, прогулку и кормление с непустыми временами; первый процесс остановлен, затем приложение запущено новым процессом на `PORT=39424` с тем же SQLite-файлом; новый `GET /` вернул `HTTP 200`, пять слотов и обе сохранённые записи. Широкая и узкая версии непосредственно перед smoke-check были проверены и приняты студентом в пункте 11.
  - Ручная проверка: студент последовательно проверил опубликованную версию `https://vibe-apps.aikibox.ru/sprint-1/`, подтвердил корректный результат итогового smoke-check и принял шаг.
  - Известные ограничения: итоговое обновление README и PLAN относится к пункту 13; проверка состава проекта относится к пункту 14.
  - Проверка:
    1. проверить дату и пять слотов;
    2. записать синтетического сотрудника;
    3. из ранее открытой страницы во втором браузере проверить отказ повторной записи;
    4. проверить сохранение после обновления;
    5. проверить состояние во втором браузере;
    6. отдельно отметить кормление и после обновления страницы проверить сохранённое состояние карточки;
    7. повторить главный сценарий на широком и узком экране.

- [x] 13. Обновить README и PLAN по фактическому состоянию

  - Статус: `Выполнено и принято студентом`.
  - Действие: привести документацию в соответствие с проверенной версией продукта.
  - Создано и изменено: `README.md` обновлён по фактическому состоянию приложения: указаны требования к Node.js, точные команды установки и запуска, поведение SQLite, главный сценарий прогулки и конфликтной записи, последнее кормление, опубликованный адрес, актуальная структура и границы учебного micro-MVP; явно указано, что решение не является промышленной системой. В `PLAN.md` записаны текущий активный шаг, границы итерации и фактический результат проверки. Новые файлы, зависимости и функции не создавались.
  - Автоматическая проверка: `npm ci` выполнен успешно, аудит сообщил `0 vulnerabilities`; `node --check database.js`, `node --check server.js` и `git diff --check` завершились успешно; приложение запущено точной основной командой из README `HOST=127.0.0.1 PORT=3000 npm start`, стартовало на `http://localhost:3000`, а `GET /` вернул `HTTP 200`, заголовок «Кто выгуливает Бориса», карточку последнего кормления и ровно пять слотов. Описание запуска, сценария, структуры и границ сопоставлено с `package.json`, `server.js`, `database.js`, `spec.md` и фактическим составом файлов; завершённые шаги 1–12 содержат результаты проверок; заявления о production-ready состоянии отсутствуют.
  - Ручная проверка: студент подтвердил результат обновления документации и принял шаг.
  - Известные ограничения: итоговая проверка состава проекта относится к шагу 14 и в этой итерации не выполнялась.
  - Проверка:
    - выполнить команду запуска из README;
    - сравнить описание с текущим кодом;
    - убедиться, что все завершённые шаги содержат фактические результаты;
    - проверить отсутствие заявлений о промышленной готовности.

- [x] 14. Проверить итоговый состав проекта

  - Статус: `Выполнено и принято студентом`.
  - Действие: просмотреть код, зависимости, документацию и итоговый diff.
  - Создано и изменено: код приложения, схема SQLite, зависимости и конфигурация запуска не изменялись; из `README.md` удалена устаревающая ссылка на пункт 14 как на следующий шаг; в `PLAN.md` записаны границы и фактические результаты итогового аудита. Новые файлы и зависимости не создавались.
  - Автоматическая проверка: просмотрены все 17 отслеживаемых Git файлов, итоговый diff и фактический список файлов; незакоммиченный diff содержит только принятые изменения `README.md` и `PLAN.md`, `git diff --check` завершился успешно. `npm ci`, `npm ls --all` и `npm audit --omit=dev` выполнены успешно: внешние runtime- и dev-зависимости отсутствуют, аудит сообщил `0 vulnerabilities`; приложение использует только встроенные модули Node.js. В отслеживаемых файлах не найдены сигнатуры приватных ключей, токенов, JWT, email-адреса или реальные ФИО; используются синтетические примеры «Анна Тестова» и «Илья Примеров». Просмотр `server.js` и `database.js` подтвердил только требуемые маршруты `GET /`, `POST /walks`, `POST /feedings`, две таблицы из `spec.md`, пять фиксированных слотов и атомарное обновление только свободного слота; функций вне продуктовых границ не найдено. `boris.sqlite`, `node_modules` и служебная `.codegraph` подтверждены как игнорируемые Git артефакты. `node --check database.js` и `node --check server.js` завершились успешно. Финальный запуск точной командой из README `HOST=127.0.0.1 PORT=3000 npm start` успешен: `GET /` вернул `HTTP 200`, заголовок приложения, карточку кормления и ровно пять фиксированных слотов; чтение `boris.sqlite` в режиме `readOnly` подтвердило обе таблицы и пять слотов даты `2026-09-17`.
  - Ручная проверка: студент подтвердил результат итоговой проверки и принял шаг.
  - Известные ограничения: публикация и commit в этой итерации не выполнялись.
  - Проверка:
    - проверить отсутствие реальных ФИО, секретов и ключей;
    - проверить отсутствие лишних функций и инфраструктуры;
    - проверить связь каждой зависимости с реализацией;
    - выполнить финальный локальный запуск.

## 4. Прямые зависимости и проверки

- `spec.md` — единственный источник продуктовых требований, SQLite-схема, семь DoD, smoke-check и границы.
- `PLAN.md` — текущий активный шаг, очередь работ, статусы и фактические результаты проверок.
- `AGENTS.md` — постоянные правила работы агента и ограничения действий.
- `README.md` — фактическое описание проекта, проверенный запуск, сценарий и границы.
- `package.json` — зависимости и предусмотренные npm-команды после создания основы проекта.
- SQLite-файл — постоянное состояние слотов и кормлений после реализации хранилища.
- Git diff — проверка фактического состава каждой итерации и отсутствия лишних изменений.
- Браузер — проверка интерфейса, главного сценария, широкого и узкого экрана.
- Второй браузер или другой контролируемый запуск — проверка общего постоянного состояния.

## 5. Критерии готовности проекта

- Приложение запускается локально командой, указанной в README.
- SQLite-файл содержит таблицы `walk_slots` и `feedings` по схеме из `spec.md`.
- Для текущей даты существуют ровно пять фиксированных слотов.
- На странице показаны текущая дата и пять слотов.
- Свободные и занятые слоты визуально различаются.
- Свободный слот можно занять корректным синтетическим ФИО.
- Занятый слот показывает ФИО первоначально записавшегося.
- Повторная запись атомарно отклоняется сервером.
- Первоначальная запись не меняется после конфликта.
- Прогулки и кормления сохраняются после обновления страницы.
- То же состояние видно во втором браузере или другом контролируемом запуске.
- Карточка последнего кормления показывает и обновляет ФИО и время.
- Главный сценарий выполняется на широком и узком экране.
- Все семь DoD подтверждены фактическими результатами.
- Каждый завершённый шаг содержит описание выполненной проверки.
- README и PLAN соответствуют текущей версии продукта.
- Просмотрен итоговый diff.
- В проекте нет функций вне `spec.md`, реальных данных и секретов.
- Границы решения и один следующий инженерный шаг указаны без заявления о промышленной готовности.
